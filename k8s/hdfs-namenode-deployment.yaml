apiVersion: apps/v1
kind: Deployment
metadata:
  name: hdfs-namenode
  labels:
    app: hdfs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hdfs
      component: namenode
  template:
    metadata:
      labels:
        app: hdfs
        component: namenode
    spec:
      containers:
      - name: namenode
        image: apache/hadoop:3.4.1
        ports:
        - containerPort: 9870
        env:
        - name: CLUSTER_NAME
          value: "hdfs-cluster"
        volumeMounts:
        - name: hdfs-config-volume
          mountPath: /opt/hadoop/etc/hadoop
      volumes:
      - name: hdfs-config-volume
        configMap:
          name: hdfs-config
      resources:
        requests:
          #cpu: "1"
          memory: "2Gi"
        limits:
          #cpu: "2"
          memory: "4Gi"
