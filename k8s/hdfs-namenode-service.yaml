apiVersion: v1
kind: Service
metadata:
  name: hdfs-namenode
spec:
  selector:
    app: hdfs
    component: namenode
  ports:
  - port: 9000
    targetPort: 9000
    name: rpc
  - port: 9870
    targetPort: 9870
    name: web
